#include <stdio.h>

int main(void){
    int i = 10; // Declare and initialize 'i' with 10
    int j = i;
    // Declare & initalize multiple variables
    int a = 2, b = 3, c = 4, d = 5;
    // %d is called format specifier
    // %d is for int, %f is for float and %c is for char

    printf("The value of i is %d and the value of j is %d", i, j);      
    return 0;
}