#include <stdio.h>

int main(void){
    int a = 4;
    int b = 3;
    int c = a + b;
    printf("The value of a is %d and value  of b is %d and sum is %d\n", a, b, c);
     // Modulas operator is used too get the remainder
     printf("The ramainder is %d", a%b);
     
    return 0;
}