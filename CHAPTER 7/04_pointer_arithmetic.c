#include <stdio.h>

int main(void){
/*
    POINTER ARITHMETIC USING INTEGER POINTER
    int a = 5;
    int* ptr = &a;
    printf("The address of a is %u\n",  ptr);
    ptr++; //Increment by 4 bits, because of integer pointer
    printf("The value of ptr is %u\n", ptr); 
    Output :-
    he address of a is 6422296
    The value of ptr is 6422300
*/

/*
    POINTER ARITHMETIC USING CHARACTER POINTER
    char a = 'A';
    char* ptr = &a;
    printf("The address of a is %u\n",  ptr);
    ptr++; //Increment by 1 bits, because of character pointer
    printf("The value of ptr is %u\n", ptr);
    Output :-
    The address of a is 6422299
    The value of ptr is 6422300
*/

    return 0;
}
